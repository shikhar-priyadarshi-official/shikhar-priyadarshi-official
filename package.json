{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test --scope={mobile,web}",
    "new-version": "lerna version --conventional-commits --yes"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
